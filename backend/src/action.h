#ifndef BACKEND_ACTION_H
#define BACKEND_ACTION_H

#include <pmp/SurfaceMesh.h>

using namespace pmp;

class Action {
public:
	static int run(const ActionArgs &action_args) {
		return 0;
	}
};

#endif //BACKEND_ACTION_H
